{
  "name": "projectswp392",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npx vite",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@vitejs/plugin-react-swc": "^3.7.0"
  }
}
