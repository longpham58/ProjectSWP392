// Cấu hình để bật/tắt mock data
// Set true để dùng mock data (không cần backend)
// Set false để dùng real API (cần backend)

export const USE_MOCK_DATA = true;

// Bạn có thể thay đổi giá trị này để switch giữa mock và real API
// Hoặc dùng environment variable:
// export const USE_MOCK_DATA = import.meta.env.VITE_USE_MOCK === 'true';
